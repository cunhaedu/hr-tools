{
  "private": true,
  "name": "hr-tools",
  "scripts": {
    "build": "turbo run build --parallel",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@rocketseat/eslint-config": "2.1.0",
    "eslint": "8.47.0",
    "tsconfig": "workspace:*",
    "turbo": "latest"
  }
}
